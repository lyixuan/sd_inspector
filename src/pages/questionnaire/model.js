import { } from './services';

export default {
  namespace: 'questionModal',
  state: {
    
  },

  effects: {
    
  },

  reducers: {
    save(state, { payload }) {
      return { ...state, ...payload };
    },
  },
  subscriptions: {},
};
