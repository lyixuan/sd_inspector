import { message } from 'antd/lib/index';
import {  } from './services';

export default {
  namespace: 'createQualityBook',

  state: {
  },

  effects: {
  },

  reducers: {
  },

  subscriptions: {
  },
};
