import { message } from 'antd/lib/index';
import {  } from './services';

export default {
  namespace: 'pointRules',

  state: {
  },

  effects: {
  },

  reducers: {
  },

  subscriptions: {
  },
};
