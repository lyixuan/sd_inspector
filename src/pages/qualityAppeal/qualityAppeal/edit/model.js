import { message } from 'antd/lib/index';
import { } from './services';

export default {
  namespace: 'EditAppeal',

  state: {
  },

  effects: {
  },

  reducers: {
  },

  subscriptions: {
  },
};
