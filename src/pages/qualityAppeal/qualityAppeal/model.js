import { message } from 'antd/lib/index';
import {  } from './services';

export default {
  namespace: 'qualityCheck',

  state: {
  },

  effects: {
  },

  reducers: {
  },

  subscriptions: {
  },
};
