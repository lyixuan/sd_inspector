import { message } from 'antd/lib/index';
import {  } from './services';

export default {
  namespace: 'createPointBook',

  state: {
  },

  effects: {
  },

  reducers: {
  },

  subscriptions: {
  },
};
