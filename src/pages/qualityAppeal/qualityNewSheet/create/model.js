import { message } from 'antd/lib/index';
import {  } from './services';

export default {
  namespace: 'createQualityNewSheet',

  state: {
  },

  effects: {
  },

  reducers: {
  },

  subscriptions: {
  },
};
