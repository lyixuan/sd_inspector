import { message } from 'antd/lib/index';
import {  } from './services';

export default {
  namespace: 'newQuality',

  state: {
  },

  effects: {
  },

  reducers: {
  },

  subscriptions: {
  },
};
